/* The one place the version is written down.
 *
 * Kept here rather than in the build files so there is nothing to drift out of
 * step: the binary reports what it was built from, whatever built it. Tags in
 * the repository are expected to match, in the form v1.0.0.
 */
#ifndef PS_VERSION_H
#define PS_VERSION_H

#define PS_VERSION "1.0.0"

#endif
